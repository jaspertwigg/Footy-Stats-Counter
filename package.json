{
  "name": "home-kitchen-app",
  "version": "1.0.0",
  "private": true,
  "description": "AI-assisted cooking & baking recipe app",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "install:all": "npm install --prefix functions && npm install --prefix web",
    "build": "npm run build --prefix functions && npm run build --prefix web",
    "typecheck": "npm run typecheck --prefix functions",
    "emulate": "npm run build && npx firebase-tools emulators:start",
    "deploy": "npm run build && npx firebase-tools deploy"
  }
}
